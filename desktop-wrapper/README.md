# MonqeyCode desktop wrapper

This wrapper starts the existing DSH web profile and displays its local URL in an Electron window.

## Build

```powershell
npm install
npm run build
```

The portable executable is written to `dist`.
